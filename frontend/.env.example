# Backend API base URL
# Replace with the actual backend server address when available
VITE_API_BASE_URL=http://localhost:8000

# Set to true to use mock API responses during frontend development
# Set to false to call the real backend API
VITE_USE_MOCK_API=false
